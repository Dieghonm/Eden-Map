import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function MissaoDay() {

  return (
    <SafeAreaView>
      <View><Text>MissaoDay</Text></View>
    </SafeAreaView>
  );
}